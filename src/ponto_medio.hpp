#pragma once

#include "outcode.hpp"
#include "point.hpp"

/*
class PontoMedio {
        friend class DrawPanel;

    public:
        void write_pixel(point_t &x, point_t &y);
        void write_circle(point_t &x, point_t &y);
        void BresenhamReta(point_t &x1, point_t &y1, point_t &x2, point_t &y2);
        void BresenhamCircun(int &r);
};
*/